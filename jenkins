pipeline {
    agent none
    options { 
        timestamps() 
        timeout(time: 15, unit: 'MINUTES') 
    }
    environment {
        VITE_BUILD_DIR = 'dist'
    }
    stages {
        stage('Checkout') {
            agent { label 'termux' }
            steps {
                sh '''
                    echo "Cleaning workspace..."
                    # Clean more thoroughly
                    rm -rf ./* ./.* 2>/dev/null || true
                    
                    echo "Cloning repository using Termux git..."
                    git clone https://github.com/HarikaGadagotti/CEM-project-devops.git .
                    
                    echo "Repository cloned successfully."
                    echo "Contents:"
                    ls -la
                '''
            }
        }

        stage('Install Dependencies') {
            agent { label 'termux' }
            steps {
                sh '''
                    echo "Running on AGENT: $(hostname)"
                    echo "Current directory: $(pwd)"
                    echo "Node version:"
                    node --version || echo "Node not found"
                    echo "NPM version:"
                    npm --version || echo "NPM not found"
                    npm ci
                '''
            }
        }

        stage('Build & Test') {
            agent { label 'termux' }
            steps {
                sh 'npm run build'
            }
        }

        stage('Archive Artifacts') {
            agent { label 'termux' }
            steps {
                archiveArtifacts artifacts: "${VITE_BUILD_DIR}/**", fingerprint: true
                echo "Vite build artifacts saved from ${VITE_BUILD_DIR}."
            }
        }
    }
}